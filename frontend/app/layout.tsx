import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Sportify - Automated Sports Tournament Scheduling',
  description: 'Automate your sports tournament scheduling with AI-powered optimization. Save time and create fair schedules for basketball, soccer, and more.',
  keywords: 'sports, tournament, scheduling, basketball, optimization, AI',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen bg-gray-50">
          {children}
        </div>
      </body>
    </html>
  )
}
